# 🛠 Manuale Operativo (Comandi Rapidi)

Guida rapida per la gestione dell'ambiente di sviluppo su Ubuntu.

## 🐍 Ambiente Python
Ogni volta che apri il terminale, attiva l'ambiente:
```bash
source venv/bin/activate
pip install -r requirements.txt